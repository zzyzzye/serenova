# 项目工程规则

本仓库遵循以下工程规则。这些规则同时适用于人工开发者和 AI Agent。

## 通用语言规则

1. Agent 与用户沟通时默认使用中文，除非用户明确要求使用其他语言。
2. 项目内新增或修改的注释、文档字符串、日志提示、调试输出、命令行打印信息，默认使用中文。
3. 如果代码、协议、第三方库接口或行业标准必须保留英文命名，可以保留英文标识符，但解释性文字仍应优先使用中文。
4. 对外接口返回内容是否使用中文，应根据业务场景决定；如果没有明确约束，内部开发向提示信息默认使用中文。

## 后端规则

1. 每个后端源码文件都必须以文件职责注释或文档字符串开头。
2. 开头说明必须清楚表达：
   - 这个文件负责什么
   - 它拥有的边界是什么
   - 必要时，它不应该处理什么
3. 当文件行为发生变化时，开头的职责说明必须在同一次改动中一并检查和更新。
4. 避免使用"工具函数""业务逻辑"这类模糊描述。说明必须具体到新成员能快速理解文件用途。
5. 后端代码必须遵循标准的分层 FastAPI 工程结构，不能继续演变成松散脚本堆积。
6. HTTP 接口放在 `app/api`，配置放在 `app/core`，数据结构放在 `app/schemas`，业务编排放在 `app/services`，持久化访问放在 `app/repositories`，数据库集成放在 `app/db`。
7. 根目录 `main.py` 只作为本地启动入口，应用装配必须放在 `app/main.py`。
8. 后端 HTTP 接口默认使用异步实现，除非具体场景确实需要同步行为。
9. 数据库访问默认使用异步驱动、异步会话和异步仓储方法，除非某个明确依赖或场景必须同步。
10. 后端默认以性能优先。在多个方案都可行时，优先选择非阻塞、异步优先、吞吐更友好的实现。
11. 如果引入同步接口或同步数据库链路，必须在变更说明或评审中明确写出原因，并且只限制在确有必要的场景里。
12. 本项目采用**模块化单体**架构：用户认证是全局共享基础层，各业务 App（video、blog、tools 等）在分层目录下各自有独立子包，彼此不得跨 App 直接调用。
13. API 路由**不使用版本前缀**（无 `/v1`）。路由结构为 `/api/{资源}`（共享层）和 `/api/{app名}/{资源}`（App 层）。
14. 新增 App 时，必须在 `api/endpoints/`、`services/`、`models/`、`schemas/`、`repositories/` 下各自创建以 App 名命名的子目录，不得将新 App 的代码混入共享层或其他 App。
15. 共享层（`core/`、`db/`、`api/deps/`、`models/user.py`、`services/auth_service.py` 等）不得引用任何具体 App 的代码。

推荐的 Python 后端文件头示例：

```python
"""处理用户登录服务编排与会话签发。"""
```

推荐的后端目录结构：

```text
app/
  api/
    deps/          # 认证依赖注入（共享）
    endpoints/
      auth.py      # 认证接口（共享）
      health.py    # 健康检查（共享）
      video/       # video app 接口
      blog/        # blog app 接口
  core/            # 配置、安全工具（共享）
  db/              # redis、postgres 连接（共享）
  models/
    user.py        # 用户模型（共享）
    video/         # video app 模型
    blog/          # blog app 模型
  repositories/
    user_repository.py   # 用户仓储（共享）
    video/
    blog/
  schemas/
    auth.py        # 认证 schema（共享）
    video/
    blog/
  services/
    auth_service.py      # 认证服务（共享）
    video/
    blog/
tests/
main.py
```

推荐的后端执行方式：

```text
- FastAPI 路由处理函数：默认使用 async def
- Service 方法：只要参与请求链路 I/O，默认使用异步
- Repository 方法：默认使用异步
- 数据库驱动和 Session：默认使用异步
```

## 前端规则

1. 前端代码必须分离 `views` 和 `components`。
2. `views` 是页面级或路由级装配层，负责页面组合、数据流协调和功能入口。
3. `components` 是可复用 UI 单元，应保持更小、更通用，不能悄悄承担整页职责。
4. 不要把页面级业务编排直接塞进共享组件里。
5. 新增前端模块必须遵循有层次的工程化结构，命名清晰，复用边界明确。
6. 本项目前端采用**多 App 模块化**结构：`views/` 和 `components/` 均按 App 名划分子目录，全局共享的页面（登录、导航门户）和组件放顶层，各 App 的页面与组件放对应子目录，不得跨 App 直接引用。
7. 新增 App 时，必须在 `views/{app名}/` 下创建该 App 的页面，在 `components/{app名}/` 下创建该 App 的专属组件，路由统一在 `router/index.js` 中按 App 分组注册。
8. 全局共享的服务（认证、HTTP 客户端）放 `services/` 顶层；若某 App 有专属服务，放 `services/{app名}/` 子目录。
9. 前端界面文案、注释和调试输出默认使用中文，除非业务本身明确要求其他语言。
10. 本项目默认前端视觉风格采用接近 iOS 的"液态玻璃"设计语言，新增页面与组件应优先保持统一的半透明、模糊、轻高光和柔和层次感。
11. 液态玻璃风格应通过真实可维护的 CSS 实现，优先使用半透明背景、`backdrop-filter`、细描边、低对比阴影和克制的渐变高光，不要只靠大面积纯色卡片替代。
12. 导航栏、浮层、弹窗、登录框、侧边面板、操作条等高层级容器优先使用玻璃质感；内容密集型列表、表格和长文区域可以适度降低透明度，保证可读性优先。
13. 颜色应保持克制和通透，优先使用浅色背景上的白色或浅灰玻璃层，并配合轻微冷暖偏移；避免高饱和霓虹色、大面积厚重纯黑和过强渐变污染内容层。
14. 圆角、阴影、边框和模糊强度应成体系复用，避免同页中出现风格差异过大的玻璃组件；新增组件时优先复用已有视觉 token 或样式变量。
15. 动效应轻、短、稳，优先使用透明度、位移、缩放和背景模糊的细微变化来表达反馈，不要使用夸张弹跳、长时间旋转或强闪烁效果。
16. 文字与图标的可读性高于视觉效果；任何玻璃面板上的正文、表单、按钮和状态信息都必须先保证对比度与聚焦态清晰，再追求风格化。
17. 背景层应服务于玻璃质感，可以使用柔和图片、渐变光斑或层次化底色，但不能让背景喧宾夺主，不能影响表单填写、数据阅读和主要操作判断。
18. 未经明确要求，不要把整个页面堆成悬浮大卡片集合；应通过留白、层次、吸附式顶部区域和少量高价值玻璃容器来体现 iOS 风格，而不是过度装饰。

推荐的前端目录结构：

```text
frontend/
  src/
    views/
      LoginView.vue      # 登录（全局共享）
      HomeView.vue       # 导航门户（全局共享）
      video/             # video app 页面
      blog/              # blog app 页面
      tools/             # tools app 页面
    components/          # 全局共享组件
      video/             # video app 专属组件
      blog/              # blog app 专属组件
    router/
      index.js           # 路由按 App 分组注册
    services/            # 全局共享服务（auth、http）
      video/             # video app 专属服务
      blog/              # blog app 专属服务
    hooks/
    utils/
```

## Docker 规则

1. Docker 支持是项目的一等能力，前后端改动都应保留清晰的容器化路径。
2. Dockerfile 默认按照生产可用标准编写，而不是只满足临时本地运行。
3. 镜像层次应保持清晰和可预测：在合适时优先安装依赖，再复制必要文件，避免构建上下文无节制膨胀。
4. 必须使用 `.dockerignore` 排除本地缓存、虚拟环境、前端依赖、测试缓存和构建产物，避免它们进入镜像上下文。
5. 后端容器必须通过正确的应用入口运行 FastAPI 服务，并与异步优先、性能优先的后端规则保持一致。
6. 前端容器在需要时应区分开发和生产场景；生产镜像默认提供构建后的静态资源，而不是直接暴露原始开发工具链，除非场景明确要求。
7. 容器配置必须来自环境变量或挂载配置，不能写死本机路径或本机专属值。
8. 如果引入 Docker Compose 或类似编排，服务、网络、卷和启动依赖必须显式、可维护。
9. 新增数据库、队列、缓存或反向代理时，其容器编排必须与仓库分层架构一致，避免隐藏耦合。
10. 默认兼顾性能和镜像效率：在可行情况下优先更小的镜像、可重复的依赖安装和更精简的运行时依赖。

推荐的 Docker 约定：

```text
- 当前后端职责不同，分别维护独立 Dockerfile
- 生产镜像优先使用多阶段构建
- 尽量让运行时镜像小于构建镜像
- 不要把 .venv、node_modules、dist、测试缓存复制进镜像
```

## 变更管理规则

1. 任何后端文件修改，都必须顺手检查开头的文件职责说明是否仍然准确。
2. 任何前端功能开发，都必须遵守 `views` 与 `components` 的职责分离，避免混用。
3. 任何 Docker 相关改动，都必须遵守上面的容器化规则，尤其是镜像整洁性、环境变量配置和可维护的服务编排。
4. 如果引入新的目录结构，也必须继续遵守这些命名和职责边界。

## SDD 工作流（Spec-Driven Development）

本项目采用 SDD 方法论进行 AI 辅助开发，使用 OpenSpec v2.x 作为规范框架。

### 规范目录
- `openspec/specs/` —— 权威基准（系统当前行为的规范）
- `openspec/changes/` —— 进行中的变更（每个变更一个文件夹）
- `openspec/changes/archive/` —— 已归档的变更

### 工作纪律
- 任何新功能/重构/修复，必须先通过 /opsx:propose 创建变更再写代码
- 实现过程中发现产物有误，先修改产物文件再修改代码
- 每个 Task 独立一次 commit，commit message 引用变更名和任务编号
- 上下文超过 40% 时，主动清理并从变更产物恢复

### 标准四产物
1. `proposal.md` —— 提案（人类主导，定义意图和范围）
2. `specs/<domain>/spec.md` —— 行为规范（AI 生成，人类审批）
3. `design.md` —— 技术方案（AI 生成，人类审批）
4. `tasks.md` —— 实现清单（AI 生成，人类审批）

### Skills 与 OpenSpec 协作
- 实现 Task 时，先读取对应的 spec 和 design，再按 Skill 规范写代码
- Skill 规定"代码怎么写"，spec 规定"代码做什么"，两者不矛盾时以 spec 为准
- 新建 Skill 时，应参考 openspec/specs/ 中的现有领域划分
